----------------------------
1.0

se coloca la opcion de colocar clave al grub al final del instalador
se agrega la opcion "HiddenServiceStatistics 1" a la configuracion de tor


----------------------------
0.9.0

se corrigen varios errores en el instalador
se agregan los siguientes paquetes: aoetools vblade lzip lzma lzop hfsutils hfsprogs disktype axel f2fs-tools zfsnap proxychains fontinstaller dconf-editor conky-all lxc galternatives xfce4-notifyd scalpel 0trace amap arp-scan hdparm fonts-hack-ttf
se eliminan los siguientes paquetes: onioncat fakeroot-ng i2p systemd-docker nfacct fonts-droid
se sustituye el paquete rkhunter por chkrootkit
se sustituye el paquete usermode por gnome-system-tools
se sustituye el paquete traceroute por mtr-tiny
se sustituye el paquete chntpw por ophcrack
se sustituye el paquete iftop por iptraf-ng
se sustituye el paquete pwgen por el script passgen
se corrigen errores en los script que utilizan yad
se trasladan algunas funciones desde variables al postinstalador
se crea un script llamado reparaciones
al instalar si hay mas de 4 GB de RAM se activan psad y fail2ban
se coloca un dialogo para preguntar si desea activar fail2ban y psad
se agrega soporte para f2fs y zfs
se cambia el nombre de la carpeta .mozilla en lugar de borrarla al ejecutar tor
se separa el paquete enlightenment y se crea el paquete low que contiene las aplicaciones ultra ligeras
se activa aide durante la optimizacion del script post-instalador
se elimina el repositorio de liquorix
se elimina del script post-installer la opcion de instalar liquorix
se agrega al script post-installer la opcion de instalar el paquete xanadu-low
si al ejecutar el script post-installer se detecta poca RAM se recomienda instalar el paquete xanadu-low
se simplifican las barras de progreso de yad 
se optimiza el script destruir
se limpia el script variables
se actualiza el paquete xanadu-repo para incluir modificaciones a los repos
se coloca "10-" al principio del nombre para asignar una prioridad de lectura al archivo xanadu-sysctl.conf
se agregan las siguientes opciones al archivo 10-xanadu-sysctl.conf: "fs.protected_hardlinks = 1" "fs.protected_symlinks = 1" "net.ipv6.conf.all.use_tempaddr = 2" "net.ipv6.conf.default.use_tempaddr = 2" "net.ipv6.route.flush = 1" "net.ipv4.route.flush = 1"
se modifican parametros en el sysctl.conf
se mejora la configuracion de polipo
se configura proxychains para usar con mi configuracion de proxies
se modifica la configuracion de privoxy para mejorar el anonimato
se agregan las variables de entorno para los proxy en bash
se bloquea todo el trafico UDP al utilizar tor
se mejora el comportamiento de apt
se actualiza el script miarranque-mem para calcular mas parametros
se elimina iptables de la lista negra
los parametros del kernel que es colocan durante el arranque ahora se escriben en /etc/sysctl.d/11-optimizaciones.conf
se agregan los script fillzero y repdisk
se agrega el paquete thermald a recomendados del postinstalador
ahora el instalador se actualiza automaticamente si posee conexion a internet
se mejora el rendimiento de los script de la distribucion
ahora en los archivos .lock se escriben los pasos satisfactorios al iniciar o apagar tor o i2p
se elimina el suid a los binarios su, wall y passwd durante la optimizacion
se agrega el paquete nvidia-detect a la funcion que instala los drivers nvidia en el post-instalador
si el instalador detecta un disco SSD activa trim
si el instalador detecta que "/" esta en XFS escribe algunos parametros necesarios en sysctl.conf
se modifica la configuracion de thunar para agregar fontinstaller
si el instalador detecta una gpu intel escribe un archivo de configuracion complementario para x11
se mejora la seguridad de iceweasel
se termina de desactivar pocket
se agregan los modulos firewire-core y thunderbolt a blacklist
de ser necesario durante el arranque se escribe "INTEL_BATCH=1" en el archivo /etc/environment
de ser necesarios se agregan parametros al grub para discos ssd y chipset i915
se agregan los siguientes paquetes firmware-myricom, firmware-misc-nonfree, firmware-ti-connectivity, firmware-qlogic
se agregan nuevas fuentes xfonts-terminus, fonts-roboto, fonts-ubuntu-title
se actualiza el repo de kali
se cambia la estructura del paquete xanadu-desktop-base para evitar conflicto con desktop-base
correcciones en los paquetes
se agrega el script swapfilescan para crear y activar archivos swap
se actualiza el script destruir


----------------------------
0.8.0

se crea una carpeta necesaria para usbmux
se agrega una lista de proxy gratuitos en iceweasel
se elimina el marcador de Kodi
se agrega http://onion.city/ a iceweasel
se agrega obfs4proxy a tor
se agrega el paquete arptables a red
se eliminan los repositorios de rivendell y kxstudio porque ya no son necesarios
se muestra el ssid y la clave del router en el caso de que ya se este ejecutando
se elimina una redundancia al limpiar iptables al iniciar el router
se corrige el comportamiento de la ventanas que muestran el mensaje de introduccion de los script
se sustituye xvncviewver por ssvnc en nav
se agrega el icono del terminal en la barra
se elimina la creacion de la carpeta /run/tor en el inicio de tor
se agrega el archivo xanadu.conf para usarlo con tmpfiles.d
se cambia el tamaño del journal
se agrega el numero de version de xanadu a los script
se eliminan los archivos f*.txt del directorio include.binary/isolinux y se traslada su creacion a su hooks
se trasladan otros archivos desde include.binary/isolinux a su hooks
se actualiza el ppa de libreoffice
ahora durante la optimizacion se elimina el paquete xanadu-installer
se corrigen varios errores ortograficos en el instalador
se reorganizan algunas dependencias en los paquetes
se mejora el script fin para que haga limpeza al cerrar sesion
ahora polipo y privoxy escriben sus log en el directorio tmp para que no se almacenen en el disco
se monta el directorio /var/log como tmpfs para evitar escrituras en disco
se agrega el paquete mdetect a xanadu-drivers
se cambian algunos parametros de montaje en el fstab
se agrega particionamiento automatico al instalador
se hacen pequeños cambios en el menu de arranque y en la ayuda
se elimina la configuracion de hdparm
se elimina el repositorio de linux mint
se crea un script para crear la particion de persistencia de forma automatica
se elimina el paquete pulseaudio-module-jack en xanadu-multimedia
se simplifica el script destruir para hacerlo mas efeciente y rapido
se crea el paquete xanadu-grubdoctor
se modifican los script para hacerlos mas pequeños
se agregan los paquetes apparmor-profiles-extra y apparmor-notify a xanadu-utilidades
se agrega la funcion wipe a thunar
se agrega el paquete pcmciautils, va-driver-all, hp-ppd a xanadu-drivers
se agrega el paquete arj a complementos
se agregan los paquetes console-tools, kbd-compat, upower a basepack
se agrega gnome-accessibility-themes a xanadu-accessibility
se coloca http://httpredir.debian.org/debian/ como repositorio principal
se crea el archivo de configuracion para agregar al usuario por defecto a ciertos grupos
se agrega el paquete rfkill a red
ahora el modo a prueba de fallos esta en español
se corrigen parametros del grub
se actualiza el repositorio de iceweasel
se agrega el paquete openbox-menu a xanadu-lxde
se agrega el paquete rhinote a xanadu-complementos
se agrega la funcion de crear un archivo para swap en el script mipersistencia
se agrega la funcion para montar el archivo swap durante el arranque
se crea un script de arranque y llamarlo desde el rc.local para trasladar allí el contenido de rc.local
en el script miarranque se llaman los diversos script de forma paralela
se agrega * soft nofile 2000 y * hard nofile 2000 a limits.conf
se activa la precarga dns via https en iceweasel
se agrega proxy checker a los marcadores del navegador
se agrega "vm.oom_kill_allocating_task=1" al sysctl
Optimizado automático en el script llamado miarranque
se elimina el paquete How-can-I-help
se modifican los siguientes parametros a estos valores: 'pref("privacy.trackingprotection.enabled", true);' y 'pref("geo.enabled", false);'
se elimina el soporte de zenity de los scripts
se agrega compatibilidad a webs de i2p para tor
Ahora durante el arranque (live) se comprueba la cantidad de ram, si esta es menor a 768 MB de ram se iniciara openbox en lugar de LXDE
ahora el instalador utiliza LVM
ahora el instalador crea y utiliza una particion /boot automaticamente
se agrega el paquete sysfsutils
se trasladaron los parametros desde el script miarranque-mem a sysfsutil
se elimina el paquete cpufrequtils porque ya no es util
se pasan variables desde el particionador al instalador
se coloco LVM como opción en el instalador
se coloca una advertencia al usar LVM
se corrige un error en la variable $seguro en post-gui-yad
se desactivan las swap en disco durante la ejecucion de tor o i2p y se reactivan al finalizar
se elimina la advertencia que dice que i2p es experimental
se agrega aspell-es a xandu-office
se agrega xul-ext-https-everywhere a xanadu-nav
se agregan los paquetes network-manager-openvpn-gnome, network-manager-vpnc-gnome, network-manager-pptp-gnome, network-manager-iodine-gnome, gufw a red
se elimina shorewall
se elimina el repositorio de kodi
se agregan elementos a la lista de excluidos del instalador para hacer la instalacion mas rapida y limpia
se modifica iceweasel para hacer peticiones DNS mediante socks
se activa la aceleracion por hardware en tor
se modifican los siguientes valores en iceweasel: pref("datareporting.healthreport.service.enabled", false); pref("datareporting.healthreport.uploadEnabled", false); pref("toolkit.telemetry.enabled", false); pref("loop.enabled", false); pref("browser.pocket.enabled", false); pref("browser.search.suggest.enabled", false); pref("media.peerconnection.enabled", false);
se elimina el repositorio de aptosid
se elimina enhanceio porque ya no existe en debian sid
se restaura el repositorio de siduction
se actualiza el kernel a 4.2.x
se crea el paquete xanadu-info cuyo contenido es el archivo os-release y la licencia
se crea el paquete xanadu-desktop-base con las dependencias que seran comunes a todos los escritorios
se coloca duckduckgo como pagina de inicio de iceweasel
se agregan los paquetes wifite y mat
se incorpora el paquete dnscrypt-proxy
se agregan nuevas configuraciones de privacidad a iceweasel
se agrega la opcion en syslinux para iniciar el bootloader instalado en el hdd
se agrega la opcion en syslinux para iniciar desde la primera particion
ahora se cierra iceweasel antes de iniciar tor
se agrega soporte para UEFI sin soporte de persistencia y solo para 64 bits en live
se agrega soporte para reiserfs
se sustituye el kernel 686-pae por un kernel sin pae
ahora al instalar en arquitecturas de 64 bits se usa GPT en lugar de MSDOS
se crea el paquete xanadu-printer para corregir un error en los drivers de impresoras
se agrega el paquete bleachbit
se elimina el paquete apparmor-profiles por ser innecesario
se hace una limpieza antes de iniciar tor
se elimina el random del wallpaper al cargar openbox
al ejecutar el particionador se selecciona automaticamente la particion root
se agrega el paquete pv a xanadu-complementos
se cambia la clave de root a: udanax
se colocan las barras de lxde y openbox transparentes
se elimina el paquete haveged porque no se utiliza
se elimina la configuracion de teclado del paquete basepack porque interfiere con el proceso de arranque en live (esto desactiva la combinacion de teclas para reiniciar el servidor X)
se divide el script complementos
se agrega una GUI para macchanger
se agrega el paquete systemd-container
se agregan los paquetes: swapspace, gtkhash, pwgen
se corrige un error al montar las swap
se agrega el paquete zenmap (una GUI para nmap)
se agrega el paquete gtkorphan
se eliminan los demas buscadores del iceweasel
se agrega el soporte para reiser4
se agrega el paquete fsarchiver, yersinia y lxlauncher
se optimizan los puntos de montaje creados por el instalador
se agrega lxmusic

----------------------------
0.7.0

Se lanza lxterminal con la combinación SUPER + t.
Se agrega comprobación en el instalador para eliminar el archivo swap si existe una partición swap.
Se agrega una comprobación de sistema de archivos al crear fstab durante el instalador y dependiendo del sistema de archivos, se colocan diferentes opciones de montaje.
Durante el instalador se activa inmediatamente la partición swap recién creada.
Se coloco una recomendación al final del instalador para ejecutar el post-instalador al reiniciar el sistema.
Durante la instalación se crea el archivo apt.conf para ejecutar prelink al final de una instalación con apt.
Se optimiza el método para generar un hostname de manera aleatoria.
Se crea una función para limpiar el cortafuegos antes y después de ejecutar cualquier cosa relacionada con tor o i2p.
Se cambia el algoritmo para generar SSID y clave del Router wifi con tor de manera aleatoria.
Se agregan los paquetes laptop-mode-tools, icedove, camorama, lightdm-gtk-greeter-settings, grub-customizer en los recomendados del post-instalador.
Se mejora el script actualizar para que actualice los keyring en caso de necesario.
Se agrega la posibilidad de actualizar las llaves de los ppa en el archivo xanadu-repo durante su actualización.
Se fusiona el script modo-server y el paquete xanadu-clean.
Durante la función office del script modo-server ahora se instala wine.
Se crea un script para hacer cambios entre versiones de Xanadu GNU/Linux llamado intermedios que sera ejecutado por el script actualizar.
Se corrigen los parámetros a escribir en fstab durante la ejecución del script intermedios.
Se sustituye liquorix por el kernel por defecto en Debian.
Se modifica el script actualizar para ejecutar la optimización la primera vez que se ejecute.
Se mejora el script destruir ahora trabaja de manera diferente desde un live y desde una instalación.
Se agregan mensajes de introducción en los script adicionales y post-instalador.
Se apaga algún posible servidor ssh y otros servicios innecesarios al iniciar tor.
Se coloca un botón para ejecutar el script actualizar en post-instalador.
Se activa la combinación ctrl + alt + backspace para reiniciar el servidor X.
Se agregar el repositorio de kodi para facilitar su instalación.
Se corrige un error en la pagina f5 de ayuda del menú de arranque.
Se actualiza el keyring de Debian multimedia.
Se activa de nuevo la búsqueda de la partición de persistencia durante el arranque para facilitar su uso al usuario.
Se incorporan los siguientes paquetes: transmission, tightvnc viewver, x11vnc, iftop, host, audacity, hostapd, hwdata, hfsplus, grsync, mtp-tools, mtpfs, catfish, pavucontrol, enhanceio, usermode, gucharmap, alsamixergui, apt-transport-tor, mesa-utils.
Se eliminan los siguientes paquetes: zsync, qjackctl, flash-player, lxmusic.
Se aumenta el nivel de compresión de la imagen iso para hacerla más pequeña.
Se corrige un error en el paquete xanadu-repo.
Se eliminan las dependencias innecesarias de los paquetes.
Se desactiva ulatency en el paquete xanadu-low.
Se desactiva zram en el paquete xanadu-low.
Se coloco debian-archive-keyring como dependencia de xanadu-archive-keyring.
Se actualiza el keyring de Xanadu GNU/Linux.
Se agrega una función para buscar archivos con catfish en thunar.
Se agrega la función para analizar en busca de virus con clamav en thunar.
Se cambiar el valor de WarnUnsafeSocks a 1 en la configuración de tor.
Se cambian los colores de las ventanas y la decoración.
Se cambian los wallpaper de grub e isolinux.
Se cambia la configuración del screensaver a blank mode, adicionalmente se bloquea la pantalla y se apaga el monitor.
Se elimina la instalación de mesa-utils durante la ejecución del script revision.
Se actualiza https-everywhere para iceweasel.

----------------------------
0.6.0.1

Se corrigen errores en el calculo de shmall y shmmax durante el arranque.
Se corrige un error de systemd que después de instalar que impide que el sistema apague o reinicie.
Se mejora la sesión openbox.
Se incorpora el script entorno dentro del script revisión.
Ahora el instalador comprueba si la pc cuenta con los recursos disponibles para la instalación.
Se sustituye brasero por xfburn.
Se sustituye xzoom por xmag.
Se mejoran las optimizaciones en el instalador.
Se hicieron algunos cambios estéticos sugeridos por nuestros usuarios.

----------------------------
0.6.0

Se sustituye el kernel original por el kernel liquorix.
Se sustituye BASH por FISH como shell por defecto.
Se agrega soporte para XFS y BTRFS además de EXT4 en el instalador.
Si se instala usando XFS se agregan parámetros dedicados al sysctl.conf.
Se incorpora apparmor.
El script “entorno” ahora nos da mas información.
Los valores “shmmax” y “shmall” se calculan durante el arranque.
Se agrega “tcp_mtu_probing” al sysctl.conf.
Se activa el soporte para IPv6.
La opción “activar seguridad” fue movida desde el script adicionales al script post-instalador y se crea la opción “desactivar seguridad”.
Se agrega la opción para instalar hplip-gui desde el post-instalador.
Se agrega la opción para desactivar compton en adicionales.
Se agrega el paquete florence (teclado en pantalla).
Se agregan los siguientes paquetes: joystick, x11-touchscreen-calibrator, xinput-calibrator, xserver-xorg-input-elographics, xserver-xorg-input-multitouch.
Se eliminan los paquetes plymouth, usbview y lxlauncher.
Se desactiva la búsqueda automática de la partición de persistencia para acelerar el arranque.

----------------------------
0.5.9

se añade el paquete “gigolo” para el manejo de conexiones a sistemas de archivos remotos.
se añade el paquete “autojump” a la opción de programas recomendados para instalar.
se incluyen los paquetes del repositorio de Xanadu, cuya utilidad sera permitirnos actualizar en subsecuentes versiones los archivos de configuración de diversos programas sin la necesidad de reempaquetar el instalador original de Debian.
se cambia la ubicación de los script de post-instalación y arranque de algunas funciones de Xanadu para cumplir con FHS.
se corrige un error en el script “revisión” que no permitía detectar correctamente la versión del kernel.
se corrigen un error en el script variables que no permitía exportar correctamente algunas funciones.

----------------------------
0.5.8

se eliminan mirrors de Debian innecesarios
se corrige error en cleanup-install que interrumpía la instalación
se modifica el script revisión para amd64
modificaciones al instalador para colocar timeout en algunos diálogos y botones de pagar y reiniciar al finalizar
se activa el script de post-install del instalador para modificar lightdm
se crea el script fin para realizar limpieza al cerrar sesión
se modifica el instalador para incorporar parte del proceso de optimización
se modifica la función optimizar en variables para reiniciar la pc al final de la optimización
se elimina la herramienta de detección de hardware del menú de arranque por redundante
cambios en el menú de ayuda
se eliminan archivos innecesarios de la carpeta isolinux
se modifican los archivos léeme para agregar la licencia del wallpaper y el logo
se corrigen errores en la configuración de compton que afectaban el rendimiento
se configura polipo para crear archivo .pid
se corrige error en la configuración de tor que desactiva las particiones swap al iniciar el servicio
lxpanel ahora es semi-transparente
se corrige error en syslinux que impide la construcción del archivo .iso
en la función optimizar en el script variables, se mejora la implementación del microcode y se activa readahead en systemd
se cambia el wallpaper y el logo
lxterminal ahora es semi-transparente

----------------------------
0.5.7

se agrega el repositorio de xanadu.
se eliminan repositorios redundantes.
se simplifican las listas de paquetes.
se cambia la prioridad de las particiones swap.
se agrega el buscador shodan a los marcadores de iceweasel.
ahora, durante el post-instalador los directorios /var/cache/apt/archives y /home/*/.cache se agregan al fstab solo si el equipo tiene mas de 4 Gb de ram.
desde ahora el post-instalador solo permite instalar algunos escritorios si el equipo posee mas de 2 Gb de ram.
se modifica el readme para eliminar información innecesaria.
se agrega “echo vm.swappiness = 10 >> /etc/sysctl.conf” a la comprobacion de memoria en la funcion “optimizar” en “variables”.
en la función “optimizar” en “variables”, se agregan mejoras para las tarjetas de video intel.
se eliminan variables mal ubicadas en los script

----------------------------
0.5.5

Repositorios
Archivos léeme y de copyright
Se agrega la función de crear archivos .pid a los script ubicados en /usr/local/bin
